package com.jobfit.resumequality;

/**
 * HIGH = likely to hurt you with almost any reader (missing contact info,
 * no experience detected at all). MEDIUM = a real weakness worth fixing
 * before you send this out. LOW = polish - worth doing, not urgent.
 */
public enum IssueSeverity {
    HIGH, MEDIUM, LOW
}
